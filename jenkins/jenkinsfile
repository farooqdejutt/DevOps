pipeline {
    agent { label 'slave'}

    stages {

                stage('build') {
            steps {
                echo 'here we will build the docker image'
                script {

                    dir('docker') { 
                    sh 'pwd'
                    sh 'ls -l'
                    sh 'docker images'
                    sh 'docker build -t myjenkin-app:v1 .'
                    sh 'docker images' 
                    }
                }
            }
        }
                stage('push') {
            steps {
            withCredentials([usernamePassword(credentialsId: docker-hub, usernameVariable: 'DOCKER_HUB_USERNAME', passwordVariable: 'DOCKER_HUB_PASSWORD')]) {
                echo 'here we will push the build to dockerhub'
                sh 'docker login -u $DOCKER_HUB_USERNAME -p $DOCKER_HUB_PASSWORD'
            }
        }
        }
                        stage('QA') {
            steps {
                echo 'here will QA the docker image'
            }
        }
    }
}
